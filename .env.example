# Copy to .env and fill in your values. Do not commit .env.

# Required for the ETL load task (MongoDB)
mongodb_conn_string=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?appName=...

# Optional: for Docker (see https://airflow.apache.org/docs/docker-stack/index.html)
# AIRFLOW_UID=50000
